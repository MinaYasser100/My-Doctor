import 'package:flutter/material.dart';

const mobileDesignSize = Size(360, 690);
const tabletDesignSize = Size(768, 1024);
const desktopDesignSize = Size(1440, 900);

class Constant {
  static const String favoritsBox = 'favorites';
  static const String popularMedicines = 'popular_medicines';
}
